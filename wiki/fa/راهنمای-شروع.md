# 🚀 راهنمای شروع کار با پلتفرم RssBot

**راه‌اندازی کامل پلتفرم Hybrid Microservices RssBot در کمتر از ۱۰ دقیقه**

## 📋 پیش‌نیازها

### نرم‌افزارهای ضروری
- **Python 3.11+** - زبان برنامه‌نویسی اصلی
- **Rye** - مدیریت dependency و virtual environment
- **Redis** - سرور cache برای service registry
- **PostgreSQL** - پایگاه داده اصلی
- **Git** - مدیریت نسخه

### نصب Rye (توصیه شده)
```bash
# نصب Rye
curl -sSf https://rye.astral.sh/get | bash
echo 'source "$HOME/.rye/env"' >> ~/.bashrc
source ~/.bashrc
```

## ⚡ شروع سریع (۳ مرحله)

### 1️⃣ دریافت و آماده‌سازی پروژه
```bash
# دانلود پروژه
git clone https://github.com/EvolveBeyond/RssBot.git
cd RssBot

# نصب dependencies
rye sync

# کپی و ویرایش فایل تنظیمات
cp .env.example .env
```

### 2️⃣ پیکربندی محیط (.env)
```bash
# ویرایش فایل .env
nano .env
```

**تنظیمات ضروری:**
```env
# Database
DATABASE_URL="postgresql://user:password@localhost/rssbot"

# Redis Cache
REDIS_URL="redis://localhost:6379/0"

# Telegram Bot
TELEGRAM_BOT_TOKEN="your_bot_token_here"

# Security
SECRET_KEY="your_super_secret_key_here"
```

### 3️⃣ راه‌اندازی سرویس‌ها
```bash
# شروع Redis
sudo systemctl start redis
# یا: redis-server

# شروع PostgreSQL
sudo systemctl start postgresql

# ایجاد پایگاه داده
createdb rssbot
```

## 🚀 اجرای پلتفرم

### روش ۱: اجرای مستقیم (توصیه شده)
```bash
python -m rssbot
```

### روش ۲: از طریق Controller Service
```bash
python services/controller_svc/main.py
```

### روش ۳: با Uvicorn
```bash
uvicorn rssbot.core.controller:create_platform_app --reload
```

## ✅ بررسی سلامت سیستم

### تست اتصال پلتفرم
```bash
# بررسی سلامت کلی
curl http://localhost:8004/health

# نتیجه مورد انتظار:
{
  "status": "healthy",
  "architecture": "per_service_core_controller",
  "services_count": 6,
  "timestamp": "2024-12-27T..."
}
```

### مشاهده لیست سرویس‌ها
```bash
curl http://localhost:8004/services

# خروجی نمونه:
[
  {
    "name": "db_svc",
    "status": "running",
    "connection_method": "router",
    "port": 8001
  },
  {
    "name": "bot_svc", 
    "status": "running",
    "connection_method": "rest",
    "port": 8002
  }
  ...
]
```

## ⚙️ پیکربندی اولیه سرویس‌ها

### تنظیم روش اتصال سرویس‌ها
```bash
# تنظیم سرویس AI به حالت router
curl -X POST http://localhost:8004/services/ai_svc/connection-method \
     -H "Content-Type: application/json" \
     -d '{"connection_method": "router"}'

# تنظیم سرویس Bot به حالت rest
curl -X POST http://localhost:8004/services/bot_svc/connection-method \
     -H "Content-Type: application/json" \
     -d '{"connection_method": "rest"}'
```

### انواع Connection Methods:
- **`router`**: اتصال مستقیم از طریق controller (سریع‌ترین)
- **`rest`**: اتصال HTTP به سرویس مستقل (مقیاس‌پذیری بالا)
- **`hybrid`**: ترکیب router و rest (انعطاف‌پذیری کامل)
- **`disabled`**: غیرفعال‌سازی سرویس

## 🎯 تست عملکرد

### تست سرویس Database
```bash
# ایجاد یک feed تست
curl -X POST http://localhost:8004/services/db_svc/feeds \
     -H "Content-Type: application/json" \
     -d '{
       "url": "https://feeds.feedburner.com/oreilly",
       "title": "O'\''Reilly Media",
       "chat_id": -1001234567890
     }'
```

### تست سرویس AI (اختیاری)
```bash
# تنظیم OpenAI API Key
export OPENAI_API_KEY="your_openai_key"

# تست پردازش متن
curl -X POST http://localhost:8004/services/ai_svc/process \
     -H "Content-Type: application/json" \
     -d '{
       "text": "Test content for AI processing",
       "action": "summarize"
     }'
```

## 🔧 مسائل رایج و حل آن‌ها

### ❌ خطای اتصال Redis
```bash
# بررسی وضعیت Redis
redis-cli ping
# باید جواب PONG بدهد

# راه‌اندازی مجدد
sudo systemctl restart redis
```

### ❌ خطای اتصال PostgreSQL
```bash
# بررسی وضعیت PostgreSQL
sudo systemctl status postgresql

# ایجاد user و database
sudo -u postgres createuser --interactive
sudo -u postgres createdb rssbot
```

### ❌ خطای dependency
```bash
# نصب مجدد dependencies
rye sync --force

# بررسی Python version
python --version  # باید 3.11+ باشد
```

### ❌ مشکل در دسترسی به portها
```bash
# بررسی portهای در حال استفاده
ss -tlnp | grep :8004

# تغییر port در .env
CONTROLLER_PORT=8005
```

## 📱 راه‌اندازی Telegram Bot

### گرفتن Bot Token
1. به [@BotFather](https://t.me/botfather) در تلگرام پیام دهید
2. دستور `/newbot` را اجرا کنید
3. نام و username برای bot انتخاب کنید
4. Token دریافتی را در `.env` قرار دهید

### تنظیم Webhook (اختیاری)
```bash
# تنظیم webhook برای production
curl -X POST http://localhost:8004/services/bot_svc/webhook \
     -H "Content-Type: application/json" \
     -d '{
       "url": "https://yourdomain.com/webhook",
       "secret_token": "your_webhook_secret"
     }'
```

## 🚀 گام‌های بعدی

### راه‌اندازی کامل شد! حالا می‌توانید:

1. **Feed اضافه کنید**: از طریق API یا admin panel
2. **Bot را کاستومایز کنید**: تنظیمات پیام‌ها و formatting
3. **سرویس‌های اضافی فعال کنید**: AI، Payment، User management
4. **مانیتورینگ راه‌اندازی کنید**: Health checks و metrics
5. **مستندات کامل مطالعه کنید**: [راهنمای توسعه](راهنمای-توسعه) و [معماری سیستم](معماری-سیستم)

### 📖 منابع مفید

- [**شروع سریع**](شروع-سریع) - راه‌اندازی در ۵ دقیقه  
- [**پیکربندی پیشرفته**](پیکربندی) - تنظیمات تخصصی
- [**راهنمای توسعه**](راهنمای-توسعه) - مشارکت در پروژه
- [**مرجع API**](مرجع-ای-پی-آی) - مستندات کامل API

---

**🎉 تبریک! پلتفرم RssBot آماده استفاده است. برای راهنمایی بیشتر، مستندات کامل را مطالعه کنید.**