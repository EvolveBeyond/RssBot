# ⚡ شروع سریع - راه‌اندازی در ۵ دقیقه

**راه‌اندازی فوری پلتفرم RssBot برای تست و آزمایش**

## 🎯 هدف

این راهنمای سریع برای توسعه‌دهندگانی است که می‌خواهند:
- ✅ **سریع** پلتفرم را تست کنند
- ✅ **بدون پیکربندی پیچیده** شروع کنند  
- ✅ **قابلیت‌های اصلی** را ببینند

## ⚡ راه‌اندازی Express (۲ دستور)

### 1️⃣ Clone و نصب
```bash
git clone https://github.com/EvolveBeyond/RssBot.git && cd RssBot && rye sync
```

### 2️⃣ اجرا با تنظیمات پیش‌فرض
```bash
python -m rssbot
```

**✅ همین! پلتفرم روی `http://localhost:8004` در دسترس است.**

## 🔍 بررسی سریع

### تست سلامت پلتفرم
```bash
curl http://localhost:8004/health
```

**پاسخ موفق:**
```json
{
  "status": "healthy",
  "architecture": "per_service_core_controller",
  "services_count": 6,
  "cache_status": "connected"
}
```

### مشاهده سرویس‌های فعال
```bash
curl http://localhost:8004/services | jq
```

**خروجی نمونه:**
```json
[
  {
    "name": "db_svc",
    "status": "running", 
    "connection_method": "router",
    "health": "healthy"
  },
  {
    "name": "ai_svc",
    "status": "available",
    "connection_method": "rest", 
    "health": "healthy"
  }
]
```

## ⚙️ تنظیمات اولیه سریع

### فعال‌سازی Cache Redis (اختیاری)
```bash
# شروع Redis local
redis-server --port 6379 --daemonize yes

# تست Redis
redis-cli ping  # باید PONG برگرداند
```

### تغییر Connection Method سرویس‌ها
```bash
# تنظیم AI service به router mode (سریع‌تر)
curl -X POST http://localhost:8004/services/ai_svc/connection-method \
  -H "Content-Type: application/json" \
  -d '{"connection_method": "router"}'

# تنظیم Bot service به rest mode (مقیاس‌پذیرتر)  
curl -X POST http://localhost:8004/services/bot_svc/connection-method \
  -H "Content-Type: application/json" \
  -d '{"connection_method": "rest"}'
```

## 🧪 تست سریع قابلیت‌ها

### 1️⃣ تست Service Discovery
```bash
# مشاهده service registry
curl http://localhost:8004/services/discovery/status

# فرس کردن cache
curl -X POST http://localhost:8004/services/discovery/refresh
```

### 2️⃣ تست Database Service
```bash
# Health check
curl http://localhost:8004/services/db_svc/health

# لیست جداول (اگر DB متصل باشد)
curl http://localhost:8004/services/db_svc/tables
```

### 3️⃣ تست Admin APIs
```bash
# دریافت تنظیمات پلتفرم
curl http://localhost:8004/admin/config

# مشاهده آمار performance
curl http://localhost:8004/admin/stats
```

## 🚀 Demo سریع ویژگی‌ها

### Hybrid Connection Mode
```bash
# تغییر سریع connection method
curl -X POST http://localhost:8004/services/ai_svc/connection-method \
  -d '{"connection_method": "hybrid"}'

# مشاهده تغییر فوری
curl http://localhost:8004/services/ai_svc/status
```

### Zero-Downtime Reconfiguration
```bash
# تغییر زنده تنظیمات (بدون restart)
curl -X POST http://localhost:8004/admin/reload-config

# بررسی عدم قطع سرویس
curl http://localhost:8004/health
```

### Redis Cache Performance
```bash
# بدون cache (کند)
time curl http://localhost:8004/services

# با cache (سریع)
time curl http://localhost:8004/services  # اجرای دوم
```

## ⚙️ پیکربندی سریع محیط

### فایل .env مینیمال
```env
# فقط موارد ضروری برای تست سریع
ENVIRONMENT=development
LOG_LEVEL=INFO
CONTROLLER_PORT=8004

# Redis (اختیاری)
REDIS_URL=redis://localhost:6379/0

# Database (SQLite برای تست)
DATABASE_URL=sqlite:///test.db
```

### اجرا با تنظیمات custom
```bash
# با port دلخواه
CONTROLLER_PORT=9000 python -m rssbot

# با log level متفاوت  
LOG_LEVEL=DEBUG python -m rssbot

# mode مخصوص testing
ENVIRONMENT=testing python -m rssbot
```

## 📊 مانیتورینگ سریع

### نظارت بر عملکرد
```bash
# آمار کلی
curl http://localhost:8004/admin/stats

# وضعیت cache
curl http://localhost:8004/admin/cache/stats

# health check همه سرویس‌ها
curl http://localhost:8004/admin/health/all
```

### لاگ‌های زنده
```bash
# مشاهده لاگ‌های controller
tail -f logs/controller.log

# لاگ‌های service discovery  
tail -f logs/discovery.log
```

## 🔧 مسائل رایج در تست سریع

### ❌ Port در حال استفاده
```bash
# تغییر port
export CONTROLLER_PORT=8005
python -m rssbot
```

### ❌ Redis در دسترس نیست
```bash
# اجرا بدون Redis (حالت SQLite)
export REDIS_URL=""
python -m rssbot
```

### ❌ Permission denied
```bash
# اجرا با دسترسی کافی
sudo python -m rssbot
# یا
chmod +x scripts/start_dev.sh && ./scripts/start_dev.sh
```

## ▶️ گام‌های بعدی

### ✅ تست موفق بود؟ حالا:

1. **📖 مطالعه [راهنمای شروع کامل](راهنمای-شروع)** - پیکربندی production
2. **🏗️ بررسی [معماری سیستم](معماری-سیستم)** - درک عمیق‌تر
3. **⚙️ [پیکربندی پیشرفته](پیکربندی)** - تنظیمات تخصصی
4. **🤖 راه‌اندازی [Bot واقعی](راهنمای-ربات)** - اتصال Telegram
5. **🚀 آمادگی [Production](استقرار-تولید)** - استقرار واقعی

### 🎯 Use Cases بعدی

- **توسعه‌دهندگان**: [راهنمای توسعه](راهنمای-توسعه)
- **مدیران سیستم**: [راهنمای استقرار](استقرار-تولید)  
- **DevOps**: [راهنمای Docker](راهنمای-داکر)

---

**🎉 عالی! پلتفرم RssBot کارکرده و آماده‌ی بررسی بیشتر است. لذت ببرید! 🚀**